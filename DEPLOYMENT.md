# 部署到 GitHub Pages 指南

## 📦 项目文件结构

```
esports-hotel-report/
├── index.html          # 主页面
├── css/
│   └── style.css       # 样式文件
├── js/
│   └── main.js         # JavaScript 脚本
├── README.md           # 项目说明
└── DEPLOYMENT.md       # 本部署指南
```

## 🚀 部署步骤

### 方法一：通过 GitHub 网页界面（推荐新手）

1. **登录 GitHub**
   - 访问 https://github.com
   - 登录您的账户

2. **创建新仓库**
   - 点击右上角 "+" 号
   - 选择 "New repository"
   - 仓库名称：`esports-hotel-report` （或任意名称）
   - 设置为 Public（公开）
   - ✅ 勾选 "Add a README file"
   - 点击 "Create repository"

3. **上传文件**
   - 进入新创建的仓库
   - 点击 "Add file" → "Upload files"
   - 将以下文件拖拽上传：
     - `index.html`
     - `css/style.css`
     - `js/main.js`
     - `README.md`
   - 点击 "Commit changes"

4. **启用 GitHub Pages**
   - 进入仓库的 "Settings"（设置）
   - 左侧菜单找到 "Pages"
   - Source 选择 "Deploy from a branch"
   - Branch 选择 "main" 分支，文件夹选择 "/ (root)"
   - 点击 "Save"

5. **等待部署**
   - 等待 1-2 分钟
   - 刷新页面，会显示访问链接
   - 格式：`https://your-username.github.io/esports-hotel-report/`

### 方法二：使用 Git 命令行

```bash
# 1. 在项目文件夹中初始化 Git
git init

# 2. 添加所有文件
git add .

# 3. 提交
git commit -m "Initial commit: 电竞酒店可行性研究报告"

# 4. 在 GitHub 创建新仓库后，添加远程仓库
git remote add origin https://github.com/YOUR-USERNAME/esports-hotel-report.git

# 5. 推送到 GitHub
git branch -M main
git push -u origin main

# 6. 然后按照方法一的步骤4启用 GitHub Pages
```

### 方法三：使用 GitHub Desktop（图形界面）

1. **下载 GitHub Desktop**
   - 访问 https://desktop.github.com/
   - 下载并安装

2. **创建仓库**
   - 打开 GitHub Desktop
   - File → New Repository
   - 填写名称：`esports-hotel-report`
   - 选择本地路径
   - 点击 "Create Repository"

3. **添加文件**
   - 将项目文件复制到仓库文件夹
   - GitHub Desktop 会自动检测变化

4. **提交并推送**
   - 在左下角填写提交信息
   - 点击 "Commit to main"
   - 点击 "Publish repository"
   - 选择 Public
   - 点击 "Publish Repository"

5. **启用 GitHub Pages**
   - 按照方法一的步骤4操作

## 🔧 部署后的配置

### 自定义域名（可选）

如果您有自己的域名：

1. 在仓库根目录创建 `CNAME` 文件
2. 文件内容填写您的域名，如：`report.yourdomain.com`
3. 在域名服务商处添加 CNAME 记录指向 `your-username.github.io`

### 更新网站

每次修改文件后：

**方法一（网页）：**
- 进入 GitHub 仓库
- 点击要修改的文件
- 点击编辑按钮（铅笔图标）
- 修改后点击 "Commit changes"

**方法二（Git）：**
```bash
git add .
git commit -m "更新内容说明"
git push
```

**方法三（GitHub Desktop）：**
- 修改本地文件
- GitHub Desktop 自动检测
- 填写提交信息并提交
- 点击 "Push origin"

## 📱 访问网站

部署成功后，访问地址为：
```
https://YOUR-USERNAME.github.io/REPOSITORY-NAME/
```

例如：
```
https://zhangsan.github.io/esports-hotel-report/
```

## ⚡ 常见问题

### 1. 网站显示 404
- 等待 5-10 分钟，GitHub Pages 需要构建时间
- 检查 Settings → Pages 中是否正确配置
- 确认仓库是 Public

### 2. 样式或脚本不加载
- 检查文件路径是否正确
- 确保 `css/` 和 `js/` 文件夹结构正确
- 清除浏览器缓存重试

### 3. 无法推送到 GitHub
- 确认 Git 已安装
- 确认已登录 GitHub
- 检查远程仓库地址是否正确

### 4. 更新后网站没变化
- 清除浏览器缓存（Ctrl+Shift+R 或 Cmd+Shift+R）
- 等待几分钟让 GitHub Pages 重新构建
- 检查 Actions 标签页查看构建状态

## 🎯 推荐的工作流程

1. **本地开发** → 在本地修改和测试
2. **提交到 GitHub** → 使用 Git 或 GitHub Desktop
3. **自动部署** → GitHub Pages 自动更新
4. **访问查看** → 打开部署的网址验证

## 📚 相关资源

- [GitHub Pages 官方文档](https://docs.github.com/pages)
- [Git 入门教程](https://git-scm.com/book/zh/v2)
- [GitHub Desktop 使用指南](https://docs.github.com/desktop)

## 💡 提示

- ✅ 确保所有文件路径使用相对路径
- ✅ 文件名避免使用中文和特殊字符
- ✅ 定期备份您的代码
- ✅ 使用有意义的提交信息

---

**部署完成后，您的电竞酒店可行性研究报告将在线可访问！** 🎉
